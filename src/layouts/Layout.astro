---
// Supports weights 100-900
import "@fontsource-variable/onest";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="storage/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-emerald-200 dark:bg-primary dark:text-[#94A3B8]">
    <div
      class="fixed top-0 z-[-2] h-screen w-screen bg-primary-950 bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.3),rgba(255,255,255,0))]"
    >
    </div>
    <slot />
  </body>
</html>
<style is:global>
  :root {
    color-scheme: dark light;
  }

  html {
    font-family: "Onest Variable", sans-serif;
    overflow-x: hidden;
    scroll-behavior: smooth;
  }
</style>
